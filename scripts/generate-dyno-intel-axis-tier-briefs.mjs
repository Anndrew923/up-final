#!/usr/bin/env node
/**
 * RETIRED — axisTierBriefs generator (blocked).
 *
 * // 由 Boss Andrew 於 2026-07 降本大會戰下令裁撤，全面改走 i18n 雙軌 sync 長評路徑，嚴禁重新生成此死碼檔案。
 *
 * Historical path (deleted): functions/dynoIntel/dynoIntelAxisTierBriefs.data.js
 * Canonical praise path: npm run dyno:sync-human-praise → dynoIntelHumanPraise.data.js
 */
import { exit } from "node:process";

console.error(
  "[blocked] generate-dyno-intel-axis-tier-briefs.mjs was retired (Boss Andrew, 2026-07). " +
    "Use `npm run dyno:sync-human-praise` — do not regenerate axisTierBriefs."
);
exit(1);
