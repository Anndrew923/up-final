/** DOM anchors for boot-sequence spotlight phases — keep in sync with Home + BottomNav. */
export const ONBOARDING_RADAR_TARGET_ID = 'onboarding-radar-target';
export const ONBOARDING_ASSESS_TARGET_ID = 'onboarding-assess-target';
