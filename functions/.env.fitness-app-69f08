# Project-specific Functions dotenv (committed — no secrets).
# Loaded by Firebase CLI on deploy for project fitness-app-69f08.
# Keep in sync with src/config/monetization.ts → leaderboardPaywallEnabled (UI messaging).
# Free ladder uploads still require an early-bird seat once claimedCount hits the server cap.
LEADERBOARD_PAYWALL_ENABLED=false
GENESIS_EARLY_BIRD_SEAT_LIMIT=2000
